import { Head, Link } from '@inertiajs/react';
import { Plus } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { create, edit } from '@/actions/App/Http/Controllers/Admin/CourseController';
import { show as builder } from '@/actions/App/Http/Controllers/Admin/CourseBuilderController';

type Course = { id: number; title: string; thumbnail_url: string | null; status: string; level: string | null; category: { name: string }; materials_count: number; contents_count: number; teachers_count: number };
export default function CoursesIndex({ courses }: { courses: { data: Course[] } }) { return <><Head title="Courses" /><div className="space-y-6 p-4 md:p-6"><div className="flex items-center justify-between"><div><h1 className="text-2xl font-semibold">Courses</h1><p className="text-muted-foreground text-sm">Manage courses and learning content.</p></div><Button asChild><Link href={create()}><Plus />Add course</Link></Button></div><div className="overflow-x-auto rounded-xl border bg-card"><table className="w-full min-w-225 text-left text-sm"><thead className="border-b bg-muted/50"><tr><th className="p-4">Course</th><th>Category</th><th>Status</th><th>Level</th><th>Materials</th><th>Contents</th><th>Teachers</th><th>Actions</th></tr></thead><tbody className="divide-y">{courses.data.map((course) => <tr key={course.id}><td className="p-4 font-medium">{course.title}</td><td>{course.category.name}</td><td>{course.status}</td><td>{course.level || '—'}</td><td>{course.materials_count}</td><td>{course.contents_count}</td><td>{course.teachers_count}</td><td className="space-x-2"><Button size="sm" variant="outline" asChild><Link href={edit(course.id)}>Edit</Link></Button><Button size="sm" asChild><Link href={builder(course.id)}>Builder</Link></Button></td></tr>)}</tbody></table></div></div></>; }
